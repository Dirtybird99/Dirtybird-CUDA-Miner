# Normalize line endings: LF in the repo for all text so build.sh / shell scripts
# run correctly on Linux/WSL regardless of the cloner's git settings.
* text=auto eol=lf
# Windows batch files use CRLF.
*.bat text eol=crlf
