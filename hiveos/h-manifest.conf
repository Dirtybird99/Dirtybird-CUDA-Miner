CUSTOM_NAME=dirtybird-cuda
CUSTOM_VERSION=0.1.2
CUSTOM_BUILD=1
# Args line generated by h-config.sh and consumed by h-run.sh.
CUSTOM_CONFIG_FILENAME=/hive/miners/custom/dirtybird-cuda/dirtybird-cuda.conf
# Miner stdout is tee'd here; h-stats.sh / mmp-stats.sh parse it (this miner has no stats API).
CUSTOM_LOG_BASENAME=/var/log/miner/dirtybird-cuda/dirtybird-cuda
